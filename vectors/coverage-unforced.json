{
  "$comment": "The honest complement to the interpretation-decision registry: normative requirements the corpus does NOT force, each classified. 'forcible-but-unforced' means a single-statement or multi-statement vector could pin it but none is written yet; 'consumer-policy-unvectorable' means the rule is trust-relative or cross-attestation and cannot be pinned by a self-contained corpus at all. scripts/coverage-matrix-gate.py generates docs/COVERAGE-MATRIX.md from this file plus the registry and fails on drift; the published counts are the measurement the standards play rests on -- 'a corpus can only force what someone thought to write a vector for' becomes a number we drive down rather than a caveat we repeat.",
  "suite": "adversarial-execution-evidence-conformance",
  "cells": [
    {
      "id": "U1",
      "requirement": "An out-of-range observationRefs index is a structural fault on ANY row, including an artifact-only row",
      "specAnchors": [
        "L907-912"
      ],
      "classification": "forcible-but-unforced",
      "reason": "The rule is basis-independent and implemented on every rail (ref-malformed / ref-out-of-range fire regardless of basis; bad-724 exercises out-of-range on an artifact row, bad-103/108 exercise wrong-type on substrate rows). The one untested cell is the wrong-TYPE variant on an artifact-only row specifically. A single-statement vector could pin it; the rule is already sound, so this is coverage completeness, not a soundness gap.",
      "witnessSketch": "bad-7xx-artifact-wrongtype-refs: an artifact-only statement whose observationRefs carries a non-integer or negative index; expected ref-malformed."
    },
    {
      "id": "U2",
      "requirement": "aeeChainScope two-sided equality gate: a consumer admits only the equal declared dimension set, neither a strictly finer (scope-narrowing) nor a strictly coarser (pooling) set",
      "specAnchors": [
        "L1525-1534"
      ],
      "classification": "consumer-policy-unvectorable",
      "reason": "The comparison is consumer policy ACROSS attestations. The conformance corpus is single-statement, so the equality gate cannot be exercised by a self-contained vector; it needs a multi-statement policy vector class (pol-001 narrowed -> reject, pol-002 pooled -> reject, pol-003 exact -> accept), designed in first-party research/163/SEAM-chainscope-narrowing-attack.md and unbuilt.",
      "witnessSketch": "A policy/ vector class over statement SETS, not single statements; requires a policy-replay harness the corpus does not currently define."
    },
    {
      "id": "U3",
      "requirement": "Set-vs-tuple reading: policy compares the declared dimension SET; the gap/fork/genesis rules key on the evaluated TUPLE, so genesis-per-subject-value is normal and only a second genesis under an identical tuple is a reset",
      "specAnchors": [
        "L1534-1541"
      ],
      "classification": "consumer-policy-unvectorable",
      "reason": "Same as U2: the distinction only manifests across two or more attestations sharing a substrate key. pol-004 (two-genesis-same-tuple -> equivocation) and pol-005 (two-genesis-different-subject -> NOT equivocation) would pin it in a multi-statement class.",
      "witnessSketch": "pol-004 / pol-005 in the unbuilt policy/ class."
    },
    {
      "id": "U4",
      "requirement": "Gap / fork / shared-predecessor / duplicate-genesis detection over a chain",
      "specAnchors": [
        "L1534-1541"
      ],
      "classification": "consumer-policy-unvectorable",
      "reason": "Chain detection is inherently cross-attestation; a single statement carries at most one chain member and cannot exhibit a gap or a fork. Belongs to the policy/ class.",
      "witnessSketch": "Multi-statement chains in the policy/ class."
    },
    {
      "id": "U5",
      "requirement": "The anchor comparison (expectedCorpusDigest / expectedSubstrateDigest) and the evidence tier are trust-relative consumer-policy facts, not validity",
      "specAnchors": [
        "L1672-1696",
        "L431-555"
      ],
      "classification": "consumer-policy-unvectorable",
      "reason": "By design these differ per consumer and are excluded from the byte-pure validity gate; a self-contained corpus vector cannot fix a per-consumer trust anchor. ok-024 exercises the tier under two pinned key policies, which is the most a single-statement vector can do; the anchor comparison itself is out of the corpus's reach by construction.",
      "witnessSketch": "None possible in a self-contained corpus; exercised only by a consumer-policy harness."
    },
    {
      "id": "U6",
      "requirement": "A key-validity window a consumer places on a named substrate key is evaluated against the armedAt inside an arming record that verifies under that key, never against issuedAt, and a statement carrying no such record is refused rather than falling back",
      "specAnchors": [
        "L1190-1206"
      ],
      "classification": "consumer-policy-unvectorable",
      "reason": "The rule constrains an operand a consumer supplies from outside the statement: it takes a key lifetime, and a corpus of self-contained single statements has no lifetime to place them in. Every statement the corpus can build is either inside or outside an arbitrary window depending on a bound the vector does not carry, so no vector can separate a rail that honours the operand from one that reads issuedAt. Recorded here rather than left implicit because the wording it replaced invited two implementers to build the defeated control independently, which is the shape a requirement takes when nothing is measuring whether anyone gets it right.",
      "witnessSketch": "A policy-replay vector supplying a key window alongside the statement, in the same unbuilt policy/ class U2 through U4 need; the accept and reject pair is one statement whose arming record verifies inside the window and one whose issuedAt alone would place it there."
    },
    {
      "id": "U7",
      "requirement": "The row-to-record assignment on a row declaring attribution: paired, which is every row whose attack the pinned corpus declares no expectedPayloads entry for: a producer MUST NOT reference a record from a row whose attack the record's committed payload does not evidence, and no conforming verifier may invent an evidencing heuristic in its place",
      "specAnchors": [
        "L892-900",
        "L1164-1173",
        "L600-609"
      ],
      "classification": "producer-obligation-ungated",
      "reason": "NARROWED, not retired, and the narrowing is what this cell predicted. The requirement used to be ungated over every row, for a reason the statement itself supplied: no record named its attack, so the honest assignment and any permutation of it differed only in the field that declared the mapping. That is no longer true wherever the corpus was willing to predict what the substrate would commit to. corpus.manifest.expectedPayloads names the expected commitment per attack, aeePayloadCommitment carries what the record committed to, and a row declaring attribution: pinned obliges the two to meet, so the same splice that used to leave the report byte-identical is now killed with attribution-pin-unmatched. That kill was proven, but against a statement built inside a rail's own test file, because no vendored vector carried two pinned rows and permuting one row is the identity: the corpus could not reach the arm, so no consumer copy was ever measured against it. ok-051-two-pinned-rows and bad-982-pinned-assignment-spliced put the splice itself in the corpus, so every rail that replays this suite is now held to the kill rather than to the three parts around it. TestPinnedAttributionKillsTheSplice stays as the rail-local control beside the null. What remains here is the other half, and it remains for the same reason as before rather than by oversight: a row declaring paired rests on a correspondence the statement carries no ground truth for, and paired is a value an honest producer may always truthfully declare, including where the corpus offers an expectation. So a producer that declares paired throughout satisfies every rule in the document and emits a statement no rail can distinguish from the honest one. The consequence still belongs beside the rule: a consumer policy that discriminates between attacks reads a mapping the producer may still permute at will on every row that declares paired. What changed is that the statement now SAYS which rows those are, in a required member, on the wire, and the closure against a producer that declares paired everywhere is therefore a consumer obligation the document states rather than a gap nothing records.",
      "witnessSketch": "None possible, and the reason is now first-principles rather than pending a revision: on a row declaring paired the statement carries no value to compare the assignment against, and a vector separating a conforming rail from one that reads the assignment would need such a rail to be conformant, which the text forbids in the same sentence. The forcible half is out of this cell and in the corpus. bad-958-pinned-row-resolves-no-interception, bad-973-pinned-clean-row, bad-959-pinned-without-expectation and bad-960-pinned-commitment-unmatched force the three parts of the pinned rule, with ok-047-attribution-pinned as the satisfied form and ok-048-attribution-paired-despite-expectation pinning that the weaker value stays truthful. That list used to be written as if it covered the splice, and it did not: every one of those vectors carries a single row, so each forces a part of the rule while the operator the rule exists to refuse was unreachable. bad-982-pinned-assignment-spliced is the operator itself, exchanging the assignment between the two pinned rows of ok-051-two-pinned-rows with the record set, every signature and the batch root left exactly as the producer signed them. The live measurement of what remains is TestCrossRowObservationRefsSpliceIsInvisible in aee/observation_refs_splice_test.go, with TestCaughtToCleanSpliceIsRejected and TestPinnedAttributionKillsTheSplice beside it as the two controls that keep the null honest from both sides."
    }
  ]
}
