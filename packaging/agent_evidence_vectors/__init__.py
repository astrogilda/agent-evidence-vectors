"""The agent-evidence-vectors distribution.

The wheel carries the reference verification rail as
``agent_evidence_vectors.run_vectors`` and every conformance corpus under
``agent_evidence_vectors/corpora/``. In a checkout the rail lives at
``packaging/run_vectors.py`` and the corpora at the repository root; this
package exists so the wheel has a name that is not ``packaging``.
"""
