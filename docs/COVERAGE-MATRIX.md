<!--
GENERATED by scripts/coverage-matrix-gate.py from vectors/interpretation-decisions.json and
vectors/coverage-unforced.json. Do not hand-edit; run the gate to regenerate. CI runs it with
--check and fails on drift.
-->
# AEE v0.6 requirement-coverage matrix

Every normative requirement, classified by whether a conformance vector forces it. The point is one
honest number: of 28 tracked requirements, **21 are forced by a vector**, 1 are
forcible but not yet vectored, 5 cannot be pinned by a self-contained corpus at all, and
1 are placed outside every gate by the specification itself. The determinacy claim rests only
on the forced rows; the other three classes are stated so a reader knows exactly where "two
implementations agree" is untested rather than confirmed.

## Coverage summary

| class | count | what it means |
|---|---|---|
| forced-by-vector | 21 | a registry decision locked by a live forcing vector |
| forcible-but-unforced | 1 | a vector could pin it; none written yet |
| consumer-policy-unvectorable | 5 | cross-attestation or trust-relative |
| producer-obligation-ungated | 1 | outside every gate by specification |
| **total tracked** | **28** |  |

## Forced by a vector

| decision | spec anchor | forcing vectors |
|---|---|---|
| D1 Referenced-record validity vs covers-nothing (two altitudes) | L542-567, L560-563, L1318-1321, L1344-1347 | `v08584c6c7d501f77`, `v6c900389fd3f9a1b`, `v2c7c29f498719ddf`, `v85caf3c6f7516ba2`, `v0cf043f1bc09bccb`, `v6b15bd108cb9c27a`, `vb730e62ac8f072b0`, `v205d5ea1079eb8e5`, `v5dcc150714259e09` |
| D2 Method cap over covering records; unknown-kind and non-covering records excluded | L565-566, L1687-1688 | `v093aa7f10f378951`, `v5a93b650d229c1bf`, `v0099f25838779fcc`, `v26db60a5d09ec82c`, `v164cf7f3f529eaff` |
| D3 Duplicate records by leaf hash | L1750-1752 | `vd70d546cef512eff` |
| D4 Fail-closed vs malformed row members | L761-764, L1060-1097, L1270-1277 | `vcac966c6b2ba6295`, `v539055896bf3a954`, `v84f40e16772e5f91`, `v3eb268f467674845` |
| D5 Clean-row actualLayer 'none' on every basis | L1277-1279 | `v8b78d15e86851925`, `v692f9605520a617d` |
| D6 Coverage completeness at class + attack granularity; assessed classes are real manifest classes | L792-794, L905-916, L963-966 | `v212f439c82b123ed`, `v5a842871df5bfe33`, `vaf114f3145d4a55a`, `vbbbcb6a491ac43c0`, `v210a8e2d17bfcc47` |
| D7 Binding-version detection: derived mismatch and explicit read-first declaration | L237-241, L241-245 | `v89cc66f8a44a1977`, `v081d9eeddbd5634f` |
| D8 One timestamp profile on both fields: RFC 3339, uppercase designators, zero UTC offset; armedAt no later than issuedAt | L1326-1327, L1784-1802 | `v6b15bd108cb9c27a`, `v05f8f5c8bb0b1d43`, `vd873c2f37814b8f8`, `v2729ba35eb041b05`, `v1b0b0cafb5ee8d08`, `v3c527c28aa2da8db`, `v65fa8117aa487fbe`, `vc9f401685f448104`, `v28d48cc69a324b81`, `v7d4d7d92a346c7a5`, `v4dfeab58a9cbdf24`, `v9a886701250e5a06` |
| D9 Strict, canonical base64 for record payloads | L1300-1318 | `v4ff6cb70764bf703` |
| D10 Evidence tier per row over covering records; DSSE any-signature-verifies; keyid never the check | L766-781, L1901-1903 | `vcf5a4601dee5c2ee`, `vfcb846afff2d6979`, `v550c91df0d36a218`, `v05e2e6ba0093d37a` |
| D11 Whole statement parsed as strict I-JSON | L100-108 | `v5155dde01a4538fd` |
| D12 Subject cardinality is unconditional (one subject on a statement of any basis) | L210-213 | `v6f6941efba2cbe28`, `v423dab49f4ed4d29` |
| D13 Duplicate attackId rows are malformed (one row per executed attack) | L920-932 | `vf14f5310b2f02875` |
| D14 Coverage sets are a disjoint partition (assessedClasses / outOfScope / routedElsewhere) | L907-916 | `v70e970d18ae52833`, `vb3c92d4ecb62bbfb`, `ved230b46692c0ada` |
| D15 String bytes are a well-formed sequence of Unicode scalar values | L110-137 | `v5717f72827e39413`, `v198c19b52cf8890e`, `v86f3484735f41acb`, `vf5599bb2dbe66e12`, `vbc7012fb2fcbf1f8`, `vb705b260669aedc7`, `vdef377fb4f572b4d`, `v5e14357c80f6239b` |
| D16 JSON nesting depth is bounded at 128 | L139-148 | `v08251931ec038e91`, `v83f4b7fe6068ef86` |
| D17 Noncharacters are rejected in every string literal (RFC 7493 section 2.1) | L110-137 | `va501d76c995de6d3`, `v0e731123ead5b96e` |
| D18 The run binding covers the carried posture object and the carried vocabulary digest | L174-182, L563-564 | `vc8e3b44ea967460b`, `v51dd58a56a16716d`, `v77fb0077815e78cc` |
| D19 networkPosture.posture is a closed registry of four values, append-only across minor versions | L847-855 | `v1d1ba5041819e108`, `vc0340fb3e51cb2b3`, `va729442fd496e59a` |
| D20 result is the minimum of three independent conditions over four values, and a claim resting on an indirect clean row ranks below the top | L435-454, L456-475 | `v14d9059f14696fe6`, `vaec3b6d70dfbbc47` |
| D21 The sealed existential ranges over the clean-row conjuncts, not only the structural members | L586-594 | `vdec5ea66f3ff63d5`, `vd538496f284b4761` |

## Forcible but not yet vectored

| requirement | spec anchor | why unforced (and the witness that would close it) |
|---|---|---|
| U1 An out-of-range observationRefs index is a structural fault on ANY row, including an artifact-only row | L947-952 | The rule is basis-independent and implemented on every rail (ref-malformed / ref-out-of-range fire regardless of basis; bad-724 exercises out-of-range on an artifact row, bad-103/108 exercise wrong-type on substrate rows). The one untested cell is the wrong-TYPE variant on an artifact-only row specifically. A single-statement vector could pin it; the rule is already sound, so this is coverage completeness, not a soundness gap. |

## Not pinnable by a self-contained corpus

| requirement | spec anchor | why unvectorable |
|---|---|---|
| U2 aeeChainScope two-sided equality gate: a consumer admits only the equal declared dimension set, neither a strictly finer (scope-narrowing) nor a strictly coarser (pooling) set | L1651-1660 | The comparison is consumer policy ACROSS attestations. The conformance corpus is single-statement, so the equality gate cannot be exercised by a self-contained vector; it needs a multi-statement policy vector class (pol-001 narrowed -> reject, pol-002 pooled -> reject, pol-003 exact -> accept), designed in first-party research/163/SEAM-chainscope-narrowing-attack.md and unbuilt. |
| U3 Set-vs-tuple reading: policy compares the declared dimension SET; the gap/fork/genesis rules key on the evaluated TUPLE, so genesis-per-subject-value is normal and only a second genesis under an identical tuple is a reset | L1660-1667 | Same as U2: the distinction only manifests across two or more attestations sharing a substrate key. pol-004 (two-genesis-same-tuple -> equivocation) and pol-005 (two-genesis-different-subject -> NOT equivocation) would pin it in a multi-statement class. |
| U4 Gap / fork / shared-predecessor / duplicate-genesis detection over a chain | L1660-1667 | Chain detection is inherently cross-attestation; a single statement carries at most one chain member and cannot exhibit a gap or a fork. Belongs to the policy/ class. |
| U5 The anchor comparison (expectedCorpusDigest / expectedSubstrateDigest) and the evidence tier are trust-relative consumer-policy facts, not validity | L1811-1835, L431-555 | By design these differ per consumer and are excluded from the byte-pure validity gate; a self-contained corpus vector cannot fix a per-consumer trust anchor. ok-024 exercises the tier under two pinned key policies, which is the most a single-statement vector can do; the anchor comparison itself is out of the corpus's reach by construction. |
| U6 A key-validity window a consumer places on a named substrate key is evaluated against the armedAt inside an arming record that verifies under that key, never against issuedAt, and a statement carrying no such record is refused rather than falling back | L1247-1263 | The rule constrains an operand a consumer supplies from outside the statement: it takes a key lifetime, and a corpus of self-contained single statements has no lifetime to place them in. Every statement the corpus can build is either inside or outside an arbitrary window depending on a bound the vector does not carry, so no vector can separate a rail that honours the operand from one that reads issuedAt. Recorded here rather than left implicit because the wording it replaced invited two implementers to build the defeated control independently, which is the shape a requirement takes when nothing is measuring whether anyone gets it right. |

## Placed outside every gate by the specification

| requirement | spec anchor | why no verifier may check it |
|---|---|---|
| U7 The row-to-record assignment on a row declaring attribution: paired, which is every row whose attack the pinned corpus declares no expectedPayloads entry for: a producer MUST NOT reference a record from a row whose attack the record's committed payload does not evidence, and no conforming verifier may invent an evidencing heuristic in its place | L932-940, L1221-1230, L614-623 | NARROWED, not retired, and the narrowing is what this cell predicted. The requirement used to be ungated over every row, for a reason the statement itself supplied: no record named its attack, so the honest assignment and any permutation of it differed only in the field that declared the mapping. That is no longer true wherever the corpus was willing to predict what the substrate would commit to. corpus.manifest.expectedPayloads names the expected commitment per attack, aeePayloadCommitment carries what the record committed to, and a row declaring attribution: pinned obliges the two to meet, so the same splice that used to leave the report byte-identical is now killed with attribution-pin-unmatched. That kill was proven, but against a statement built inside a rail's own test file, because no vendored vector carried two pinned rows and permuting one row is the identity: the corpus could not reach the arm, so no consumer copy was ever measured against it. vd1aa74416099f3f0 and v6ed1e2626b2af5ad put the splice itself in the corpus, so every rail that replays this suite is now held to the kill rather than to the three parts around it. TestPinnedAttributionKillsTheSplice stays as the rail-local control beside the null. What remains here is the other half, and it remains for the same reason as before rather than by oversight: a row declaring paired rests on a correspondence the statement carries no ground truth for, and paired is a value an honest producer may always truthfully declare, including where the corpus offers an expectation. So a producer that declares paired throughout satisfies every rule in the document and emits a statement no rail can distinguish from the honest one. The consequence still belongs beside the rule: a consumer policy that discriminates between attacks reads a mapping the producer may still permute at will on every row that declares paired. What changed is that the statement now SAYS which rows those are, in a required member, on the wire, and the closure against a producer that declares paired everywhere is therefore a consumer obligation the document states rather than a gap nothing records. |
